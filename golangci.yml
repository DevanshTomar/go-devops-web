run:
  go: '1.20'  # Update this to match your Go version

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
  disable-all: false

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck